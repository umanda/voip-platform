; lab/asterisk/conf/rtp.conf — RTP port range for Asterisk

[general]
; Port range for audio streams — keep narrow in lab to reduce firewall noise
rtpstart=20000
rtpend=20200

; DTMF RFC 2833 payload type
dtmftimeout=3000
rtpchecksums=yes

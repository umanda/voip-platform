# Production-only dependencies — no test tools.
# Used by Dockerfile for the production/staging image.
# For local development and testing, use requirements.txt (includes pytest etc.)

fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.0
pydantic-settings==2.6.0
redis[hiredis]==5.2.0
structlog==24.4.0
anyio==4.7.0
